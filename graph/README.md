# Graph

The graph package is intentionally retained as a small transitional adapter.

The canonical organization graph should eventually be emitted as data (JSON) and Mermaid (MMD), not rendered by TUI logic.

See issue #2 for the migration plan.
